Each file is in the following format:

<sender> <sender's sequence number> <signal strength> <local seq. no.> <global time>

The contact graph can be reconstructed by using the sender and the global time. The other values can be safely ignored.